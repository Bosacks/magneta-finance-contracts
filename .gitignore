node_modules/
cache/
artifacts/
typechain-types/
deployments/local*.json
.env
.env.local
.env.*
!.env.example
coverage/
coverage.json

# Foundry build output
out/
broadcast/

# Package lockfiles in sub-packages — generated by pnpm workspace with link:true entries
# that break standalone npm install in CI. Let CI regenerate them fresh.
packages/*/package-lock.json
